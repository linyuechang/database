# 云笺备忘录

一个无需后端的网页版备忘录示例，支持三类记录：

- 备忘录：记录灵感、清单和提醒。
- 日记：记录每日复盘和心情。
- 日程安排：记录会议、活动和未来计划。

## 使用方式

直接用浏览器打开 `index.html`，或者在本目录启动一个静态服务：

```bash
python3 -m http.server 8000
```

然后访问 <http://localhost:8000>。记录会通过 `localStorage` 保存在当前浏览器中。
